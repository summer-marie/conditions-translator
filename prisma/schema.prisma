// Phase 1: no models yet. Data models are added in Phase 2
// (Schema, ORM, and Ownership) per docs/08_Conditions_Translator_Implementation_Roadmap.md.

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}
